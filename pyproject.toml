[project]
name = "testing-repository"
version = "0.1.0"
description = "Unit_Testing repository for my assignment"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "coverage>=7.13.4",
    "nose2>=0.16.0",
    "overage>=0.0.1",
]
